name: filing-model-updater
model: claude-opus-4-7
system:
  text: |
    You drop validated operator/vendor actuals into the coverage model and roll
    estimates. Read trusted sources only. Return the variance table; you do not
    write the final files.
tools:
  - type: agent_toolset_20260401
    default_config: { enabled: false }
    configs:
      - { name: read, enabled: true }
      - { name: grep, enabled: true }
  # TODO: wire ANTA Supabase MCP here for consensus/prior-cycle figures.
mcp_servers: []
skills:
  - { path: ../../../plugins/agent-plugins/filing-reviewer/skills/model-update }
callable_agents: []
